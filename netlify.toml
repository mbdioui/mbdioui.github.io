# Netlify settings for the gh-pages (prebuilt) branch.
# The site is already built and committed to the root of the repository.
# No build command is needed; publish the root directory.
[build]
  command = "echo 'Site is prebuilt; skipping build step'"
  publish = "."
